// const authPage = document.querySelector('.auth');

// if (authPage) {
//   const authBtn = document.querySelector('.auth-form__submit');

//   authBtn.setAttribute('disabled', 'disabled');

//   setTimeout(() => {
//     authBtn.removeAttribute('disabled');
//   }, 1000);
// }
